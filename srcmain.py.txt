from src import data_scraper, data_parser, database, youtube_transcripts

def main():
    raw_data = data_scraper.scrape_all()
    parsed_data = data_parser.parse(raw_data)
    database.store(parsed_data)
    youtube_transcripts.extract_and_store()

if __name__ == "__main__":
    main()